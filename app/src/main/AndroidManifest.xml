<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.group1.servicesapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ServicesApp">

        <activity
            android:name=".UI.Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".UI.AddService" />
        <activity android:name=".UI.ServiceDescription"/>
        <activity android:name=".UI.EditService"/>


        <activity
            android:name=".UI.ChooseAccount">
        </activity>

        <activity
            android:name=".UI.ProcessPayment">
        </activity>

        <activity
            android:name=".UI.RegisterBusiness">
        </activity>

        <activity
            android:name=".UI.RegisterUserAccount">
        </activity>

        <activity
            android:name=".UI.ViewUserProfile">
        </activity>

        <activity
            android:name=".UI.EditUserProfile">
        </activity>

        <activity
            android:name=".UI.ViewBusinessProfile">
        </activity>

        <activity
            android:name=".UI.EditBusinessProfile">
        </activity>

        <activity
            android:name=".UI.ChangePassword">
        </activity>

        <activity
            android:name=".UI.LeaveReview">
        </activity>

        <activity
            android:name=".UI.BrowseReview">
        </activity>

        <activity
            android:name=".UI.BrowseFavorite">
        </activity>

        <activity
            android:name=".UI.OrderBrowse">
        </activity>

        <activity
            android:name=".UI.ReportDescription">
        </activity>

        <activity
            android:name=".UI.ViewCurrOrder">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".UI.OrderDescription">
        </activity>

        <activity
            android:name=".UI.ScheduleDateTime">
        </activity>

        <activity
            android:name=".UI.OrderConfirm">
        </activity>

        <activity android:name=".UI.ServiceBrowse"/>
    </application>

</manifest>